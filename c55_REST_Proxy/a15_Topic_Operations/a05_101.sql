

    # Topic operations than can be executed using REST Proxy

        + Get list of topics
            GET /topics

        + Get a specific topic
            GET /topics/topic_name

    # Topic operations not possible with REST Proxy

        + Cannot create / configure topic
          (Possible only with command line )