


    # Types of joins supported
        + inner
        + left join
        + outer join

    # Joins can also be performed as,
        + Windowed (Within a specific time-frame)
          . KStream - KStream is always windowed
        + Non-windowed
          . Other combination of join are non-windowed

    # Limitations
        + https://docs.confluent.io/3.2.2/streams/developer-guide/dsl-api.html#joining