

== reduce() ==

# Similar to aggregate, but input & output type should be same

-- KGroupedStream

    kGroupedStream.reduce(...)

-- KGroupedTable

    kGroupedTable.reduce(...
    <subtractor>
    ...)