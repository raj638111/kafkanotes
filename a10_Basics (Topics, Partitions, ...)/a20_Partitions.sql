
-- Partitions

  + A Topic is divided into Partitions
  + No of partitions are specified when creating a topic 
  + Ordered
    - Each partition in a topic is ordered (example: partition 0, partition 1, ...)
