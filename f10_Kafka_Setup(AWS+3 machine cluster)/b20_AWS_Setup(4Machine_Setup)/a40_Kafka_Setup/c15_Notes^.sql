
  # 31

  # We have not used Elastic Public IP in our setup...
    We have only used Elastic private IP in our setup
    (To access we need to use the Public IP ^^ although we have done our
     settings using Elastic Private IP)

  # When using Elastic Public IP, we just need to change the
    /etc/hosts with the Elastic Public IP

