
-- max.poll.records

  # Max no of records that can be fetched in each poll request
    + Default = 500
  # Increase this, if we get a lot of small messages (And have lot of RAM)
  # (Good to monitor this)