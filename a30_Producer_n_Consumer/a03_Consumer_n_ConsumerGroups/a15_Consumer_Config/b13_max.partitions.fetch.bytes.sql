
-- max.partitions.fetch.bytes

  # Maximum data returned by broker per partition

    + Default = 1 MB
    + If the consumer is reading from 100 partition, then the consumer
      would need to have 100MB of RAM