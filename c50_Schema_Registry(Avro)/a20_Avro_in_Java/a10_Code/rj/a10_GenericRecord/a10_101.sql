
-- GenericRecord

    # Used to create an Avro Object from a schema

    # Schema can be referenced as a,
        + file
        + string

    # Advantage
        + Is simple

    # Disadvantage
        + Is not most recommended, as creating Avro object
          can fail at **runtime (ie not type safe)

